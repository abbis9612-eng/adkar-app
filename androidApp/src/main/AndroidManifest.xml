<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        الإنترنت: لتنزيل خطوط المصحف وحدَها، لا لغيرها.

        وكان التطبيق بلا هذه الصلاحية إطلاقاً — وهو أمرٌ حسنٌ في نفسه:
        تطبيقٌ لا يملك فتحَ اتّصالٍ لا يُسأل عمّا يرسل. فلمّا طُلبت
        الصفحةُ المصحفية وجب تنزيلُ خطوطها، وبدونها كان التنزيل يفشل
        بـ«Permission denied» بلا أن يفهم أحدٌ لماذا.

        وكلُّ ما عدا الخطوط يبقى دون إنترنت: المصحفُ والتفسيرُ والأذكارُ
        والأدعيةُ والمواقيتُ والقبلةُ — كلُّها محسوبةٌ أو مشحونةٌ في
        الحزمة. ولا يُفتح اتّصالٌ إلّا بإذنٍ صريحٍ من صاحبه.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass"       android:required="false" />
    <uses-feature android:name="android.hardware.location.gps"        android:required="false" />

    <application
        android:name=".RafiqApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RafiqAlDhikr"
        android:localeConfig="@xml/locales_config"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.RafiqAlDhikr.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="rafiqaldhikr.app" />
            </intent-filter>
        </activity>

        <!--
            حفظُ اللغة على أندرويد ١٢ فأقلّ.

            من ١٣ فأعلى يحفظ النظامُ لغةَ التطبيق بنفسه. وما دونها يحفظها
            AppCompat — لكن فقط إن وُجدت هذه الخدمةُ الحاملةُ للوسم. وهي
            مفقودة، و`minSdk` هنا ٢٣ — فكان اختيارُ الإنجليزية يضيع عند كل
            إقلاع، ويعود التطبيقُ عربياً، ولا يفهم صاحبُه لماذا.
        -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- مشاركةُ ملفّ التصدير بأمان — انظر res/xml/file_paths.xml -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver android:name=".service.PrayerAlarmReceiver" android:exported="false" />

        <!--
            كلُّ حدثٍ يُبطل التنبيهات المجدولة يجب أن يُعيد جدولتها.

            وكان الفلترُ يحمل `BOOT_COMPLETED` وحدَه — فيُعاد الجدولةُ بعد
            إقلاع الجهاز لا غير. أمّا:

            · MY_PACKAGE_REPLACED — أندرويد يُلغي كلَّ تنبيهات التطبيق عند
              كل تحديث. فكان المستخدم يفقد الأذانَ بعد كل تحديثٍ من المتجر
              ولا يعود إلّا إن فتح التطبيق بنفسه.

            · TIME_SET و TIMEZONE_CHANGED — التنبيهاتُ مجدولةٌ بتوقيتٍ
              مطلقٍ (`RTC_WAKEUP`) محسوبٍ من المنطقة الزمنية الحاضرة. فمن
              سافر بقيت عنده مواقيتُ بلدِه: يؤذَّن الفجرُ في العصر.

            و LOCKED_BOOT_COMPLETED لأجهزةٍ تُقلع إلى التخزين المحميّ أوّلاً.
        -->
        <receiver android:name=".service.BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!--
            يُمنح إذنُ التنبيه الدقيق من إعدادات النظام بعد أن يكون
            التطبيق قد جدول تنبيهاتٍ تقريبية. وبلا هذا المستقبِل تبقى
            التقريبيةُ إلى أن يُفتح التطبيق مرّةً أخرى.
        -->
        <receiver android:name=".service.ExactAlarmPermissionReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver android:name=".widget.PrayerWidgetReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_info" />
        </receiver>

    </application>
</manifest>
