# تقرير التدقيق المحدّث — رفيق الذكر
**التاريخ:** 2026-07-18 · **المدقّق:** فحص شامل للكود (androidApp + shared) · **المرجع السابق:** `REVIEW_REPORT.md` (2026-07-02)

**الحكم العام:** تقدّم كبير جداً منذ التقرير السابق — **7 من 8 مشاكل حرجة قديمة انصلحت وتم التحقق منها في الكود**. المتبقي: مشكلتان حرجتان جديدتا التصنيف، وعدد من المتوسطات، وتنظيف كود يتيم.

---

## القسم 1 — حالة المشاكل الحرجة القديمة (تحقق سطر بسطر)

| # | المشكلة القديمة | الحالة اليوم | الدليل |
|---|----------------|--------------|--------|
| 1 | زر "حذف كل شيء" لا يحذف | ✅ **انصلحت** | `ExportDataScreen.kt:191` يستدعي `viewModel.deleteAllData` → `UserDataRepositoryImpl.kt:188` `clearAllUserData()` يحذف فعلياً داخل transaction (progress, streaks, tasbeeh, prayerLog...) |
| 2 | تصدير البيانات وهمي | ✅ **انصلحت** | `ExportDataViewModel.kt:12` → `userDataRepo.exportAsJson()` حقيقي |
| 3 | خلفية Scaffold مثبتة بيج | ✅ **انصلحت** | لا وجود لـ `0xFFF4EFE5` في `MainActivity.kt` |
| 4 | اختيار الثيم غير موصول | ✅ **انصلحت** | `MainActivity.kt:51-57` — `darkTheme = when(themePref) { ... else -> isSystemInDarkTheme() }` ويمرَّر لـ `RafiqTheme` |
| 5 | الخواطر 30/366 → شاشة فارغة | ✅ **انصلحت جزئياً** | `KhatiraRepositoryImpl.kt:37-44` — fallback بالدوران `(dayOfYear-1) % count + 1` فلا شاشة فارغة. **لكن** المحتوى ما زال 30 خاطرة تتكرر كل شهر (انظر 🟠 أدناه) |
| 6 | أقسام أدعية فارغة بأرقام مزيفة | ✅ **انصلحت** | `duas.json` صار 27 دعاء في 9 أقسام، والعدّ حقيقي من قاعدة البيانات (`duaCountLabel(count)` في `DuaCategoriesScreen.kt:66`) |
| 7 | إشعارات الأذان يوم واحد فقط | ✅ **انصلحت** | `PrayerAlarmReceiver.kt:33` — بعد آخر إشعار يستدعي `rescheduleTomorrow()` + وجود `PrayerRescheduler.kt` |
| 8 | `gradlew` يونكس مفقود | ✅ **انصلحت** | موجود وقابل للتنفيذ في الجذر |

### مشاكل متوسطة قديمة انصلحت أيضاً (تم التحقق)
- ✅ الروابط العميقة: `navDeepLink` مضاف للوجهات (`RafiqNavGraph.kt:89,101,109,122`)
- ✅ اختيار القارئ: موصول فعلاً بالرابط (`QuranAudioPlayer.kt:57`)
- ✅ التفسير الميسر: `tafsir_muyassar.json` موجود ويُزرع في قاعدة البيانات (`DatabaseSeeder.kt:80`) ولا وجود لنص "تحديث قادم" في أي شاشة
- ✅ التاريخ الهجري: `IslamicCalendar` بحساب أم القرى (`HomeViewModel.kt:281-287`)
- ✅ الإنجازات دائمة: جدول `Achievement.sq` مع `unlocked_at` و`INSERT OR IGNORE`
- ✅ إعدادات الإشعارات: موصولة بـ `SettingsViewModel` عبر Koin (لم تعد `remember` محلية)
- ✅ الـ Onboarding يطلب إذن الإشعارات (`OnboardingScreen.kt:96-101`) و`LocationPermissionEffect` مستخدم في `HomeScreen`
- ✅ العد التنازلي يعيد الحساب عند الصفر (`HomeViewModel.kt:196-200`)
- ✅ خط القرآن: الآيات تُعرض بخط Scheherazade (خط مصحفي حقيقي) بدل IBM Plex (`QuranReadingScreen.kt:214,291,345,397`)
- ✅ محتوى الأذكار زاد: الصباح 18، المساء 14، الصلاة 10، النوم 8، متفرقة 6

---

## القسم 2 — المشاكل المتبقية اليوم

### 🔴 حرجة

| # | المشكلة | الملف:السطر | الدليل | الحل المقترح |
|---|---------|-------------|--------|---------------|
| 1 | **شاشة Premium وهمية بالكامل** — زر "استعادة المشتريات" فارغ `onClick = { /* restore */ }`، و`purchase()` و`restore()` في الـ ViewModel مجرد `/* placeholder */`. المستخدم قد يظن أنه اشترى/استعاد وهو لم يحدث شيء — مشكلة ثقة ومتاجر التطبيقات ترفضها | `PremiumScreen.kt:115` + `PremiumViewModel.kt:20-21` | `fun purchase() { /* placeholder */ }` | **قبل الإطلاق:** إمّا إكمال ربط RevenueCat أو إخفاء الشاشة/الزر كلياً من الإصدار الأول |
| 2 | **الموقع الافتراضي صامت (السليمانية)** — بدون إذن الموقع تُعرض مواقيت صلاة لمدينة أخرى دون أي تنبيه. لا يوجد أي شارة "حدد موقعك" في الكود (بحث شامل: صفر نتائج). في تطبيق صلاة هذا خطأ عبادي حقيقي | `HomeViewModel.kt:63-64, 93-94` | `val lat = prefs.lastKnownLat.takeIf { it != 0.0 } ?: 35.5558` | شارة/بانر واضح على الرئيسية وشاشة المواقيت: "المواقيت تقريبية — حدد موقعك" مع زر طلب الإذن |

### 🟠 متوسطة

| # | المشكلة | المكان | التفصيل | الحل |
|---|---------|--------|---------|------|
| 3 | **الخواطر تتكرر كل 30 يوماً** | `khatira_366.json` (30 عنصراً من 366) | الـ fallback يمنع الشاشة الفارغة لكن المستخدم الملتزم سيلاحظ التكرار الشهري | إكمال 366 خاطرة مراجَعة شرعياً (دفعات: 90 ثم 180 ثم 366) |
| 4 | **محتوى أدعية ما زال رقيقاً في قسمين** | `duas.json` | morning: دعاء واحد، istikharah: دعاء واحد | إكمال من حصن المسلم بمصدر ودرجة |
| 5 | **الخط "العثماني" المزيف ما زال مستخدماً** | `AppTypography.kt:46-48` + `QuranSearchScreen.kt:30` | `kfgqpc_uthmanic_hafs.ttf` (وهو Amiri معاد تسميته) يُستخدم في بحث القرآن | إمّا استبدال الملف بخط KFGQPC الأصلي من مجمع الملك فهد، أو توحيد بحث القرآن على `QuranFamily` (Scheherazade) وحذف الملف المزيف |
| 6 | **ألوان مثبتة في مكوّن مستخدم** | `PrayerScene.kt` (12 لوناً `Color(0x...)`) | مكوّن مستخدم فعلاً — ألوان سماء/مشهد ثابتة قد تكون مقصودة فنياً لكنها خارج نظام الثيم | مراجعة بصرية بالوضع الداكن؛ إن كسرت المشهد → نقلها لـ `RafiqPalette` |
| 7 | **TODOs بنية تحتية** | `RafiqApplication.kt:58,61` | Crashlytics بلا `google-services.json`، RevenueCat بلا مفتاح | قرار قبل الإطلاق: تفعيل أو إزالة الكود المعلّق |

### 🟡 تنظيف (كود يتيم — 10 ملفات غير مستخدمة إطلاقاً)

تحقق بالبحث عن الاستدعاءات: **صفر استخدام** لكل من:
`PrayerSkyBackground` (فيه 32 لوناً مثبتاً!)، `ShimmerPlaceholder`، `OfflineBanner`، `RatingDialog`، `HomeTabletScreen`، `QuranTabletScreen`، `FoldableLayout`، `WearScreens`، `BismillahOrnament`، `OrnamentalBorder`

**التوصية:** `OfflineBanner` و`RatingDialog` قيّمان — اربطهما. الباقي احذفه (يقلّص الصيانة ويزيل 30+ لوناً مثبتاً دفعة واحدة).

> ملاحظة: لم يُنفَّذ بناء `assembleDebug` في بيئة هذا الفحص (لا يتوفر Android SDK فيها) — الفحص أعلاه ثابت بقراءة الكود، وكل نتيجة موثقة بمسار وسطر.

---

## القسم 3 — خطة العمل المقترحة (بالترتيب)

كل خطوة = تعديل واحد صغير → بناء → فحص بصري بالوضعين → commit:

1. **إخفاء/تعطيل شاشة Premium** من الإصدار الأول (أو ربط RevenueCat إن كان المفتاح جاهزاً) — 🔴 #1
2. **شارة "حدد موقعك"** على الرئيسية عند استخدام الموقع الاحتياطي — 🔴 #2
3. **ربط `OfflineBanner`** (جاهز ومكتوب) بالشاشات التي تلمس الشبكة — 🟡
4. **حذف الكود اليتيم** (8 ملفات) — 🟡
5. **حسم الخط العثماني** — استبدال أو توحيد على Scheherazade — 🟠 #5
6. **محتوى**: دفعة خواطر + أدعية الصباح والاستخارة (بمصادر موثقة يوفرها صاحب المشروع) — 🟠 #3-4
7. مراجعة `PrayerScene` بالوضع الداكن — 🟠 #6

---

## القسم 4 — طاقم الوكلاء الجديد

اعتباراً من هذا التاريخ أُضيف للمشروع طاقم `.claude/agents/`:

| الوكيل | الملف | الدور | الصلاحيات |
|--------|------|------|-----------|
| 🔍 المدقّق | `code-auditor.md` | يكتشف الأخطاء فقط، لا يعدّل | قراءة فقط (Read/Grep/Glob/Bash) |
| 🛠️ المنفّذ | `implementer.md` | تغيير واحد صغير + بناء إجباري بعده | تعديل كامل بقانون صارم |
| 🎨 خبير الواجهة | `ui-expert.md` | تحسين UI/UX ضمن الهوية (زمردي+ذهبي+كريمي)، RTL، الوضعان معاً | تعديل بموافقة |

**قانون الشغل:** تغيير واحد → بناء → فحص بالوضعين فاتح/داكن → موافقة المستخدم → commit → التالي.
