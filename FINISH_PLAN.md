# خطة الإنهاء — من ٤٥ شاشة إلى تطبيق واحد

> وثيقة قرار. ليست اقتراحات. كل بند فيها إمّا مُتحقَّق منه بالكود أو مُعلَّم بأنه رأي.
> تاريخ التحقق: البناء ٤٥ · الكومِت `8da130a`

---

## الجزء ١ — التحقق من خطتك، بنداً بنداً

| # | ما قالته خطتك | الحكم | الدليل في الكود |
|---|---|---|---|
| 1 | أزرار «المميز» غير فعّالة | ✅ صحيح — لكنه **مُعالَج أصلاً** | `PremiumViewModel.kt:20-21` placeholder، و`SettingsScreen.kt:64` يخفي المدخل منذ فترة |
| 2 | fallback صامت لإحداثيات السليمانية | ✅ صحيح — و**أسوأ مما وصفتَ** | ٧ ملفات بـ `35.5558/45.4436`، و`GetPrayerTimesUseCase.kt` بـ `21.3891/39.8579` (مكة). **احتياطيّان مختلفان في تطبيق واحد** |
| 3 | ٨ لغات معروضة ومواردٌ للغتين | ✅ صحيح | `locales_config.xml` فيه `ar en fr tr ur id ms bn`؛ الموجود فعلاً `values` و`values-en` فقط |
| 4 | نصوص عربية مكتوبة في Kotlin | ✅ صحيح — و**أخطر مما وصفتَ** | `stringResource(` مستخدَمة **١٢ مرة** في التطبيق كلّه، مقابل **~٧٠٠** سلسلة نصية عربية مضمّنة في Kotlin. الترجمة ليست ناقصة، هي **غير موجودة بنيوياً** |
| 5 | `compileSdk=36` مقابل `targetSdk=35` | ✅ صحيح | `androidApp/build.gradle.kts:16,21` |
| 6 | النسخة الإصدارية تسقط إلى توقيع debug | ✅ صحيح | `build.gradle.kts:47-51` — `if (KEYSTORE_FILE != null) … else signingConfigs.getByName("debug")`. تبني release موقّعة بمفتاح debug **بصمت** |
| 7 | إذنا التنبيه الدقيق كلاهما مطلوبان | ✅ صحيح | `AndroidManifest.xml:8-9` — `SCHEDULE_EXACT_ALARM` **و** `USE_EXACT_ALARM` معاً. الثاني يتطلّب تبريراً في Play، والأول يصبح زائداً معه |
| 8 | CI يبني assembleDebug فقط بلا lint ولا اختبارات | ✅ صحيح | `build-apk.yml:28` خطوة بناء واحدة. **ولا يوجد اختبار واحد في المستودع** (٠ ملف) رغم أن `junit/mockk/turbine/compose-ui-test` كلها معرَّفة في `build.gradle.kts:140-146` |
| 9 | ترحيلات القاعدة تبتلع الاستثناءات | ✅ صحيح | `DatabaseDriverFactory.kt:27` — `try { … } catch (_: Exception) {}`. عمود ناقص = شاشة تنهار لاحقاً بلا أثر في السجل |
| 10 | سياسة الخصوصية تدّعي تشفير SQLite | ✅ صحيح — و**فيها كذبتان لا واحدة** | `PrivacyPolicyScreen.kt:88`. القاعدة `AndroidSqliteDriver` عادية بلا SQLCipher. و`createEncryptedPrefs()` في `SecurityModule.kt` **كود ميت لا يُستدعى من أي مكان** ولا يُسجَّل في Koin (`RafiqApplication.kt:31`). والجملة نفسها تعد بـ«ميزة المزامنة» وهي **غير موجودة أصلاً** |
| 11 | `allowBackup=true` | ✅ صحيح | `AndroidManifest.xml:22` |
| 12 | `MainActivity` مقفلة عمودياً | ✅ صحيح | `AndroidManifest.xml:34` — `screenOrientation="portrait"`. مرفوض على أندرويد ١٦ للشاشات الكبيرة |
| 13 | التنقّل يجب أن يصير «اليوم \| القرآن \| الأذكار \| الأدعية \| المزيد» | ⚠️ **مُنفَّذ أصلاً** بخمسة تبويبات، آخرها «أوراقي» بدل «المزيد» | `RafiqBottomBar.kt:42-48` |
| 14 | إعادة هندسة الرئيسية ببطل واحد | ⚠️ **مُنفَّذ أصلاً** | `WaraqaScreen.kt` — استبدلت لوحة الـ٧ بطاقات القديمة |
| 15 | «النظام التصميمي غير موحّد» | ❌ **قديم/غير صحيح** | م١–م٧ منفَّذة: ٣٧٢/٣٧٢ نص بارتفاع سطر عربي، ٠ ظلّ، ٥ أشكال، ٣٢ توكن لون، كل زوج يعبر عتبة WCAG |

**الخلاصة: ١٢ من ١٥ بنداً صحيحة، ٢ منفَّذان أصلاً، ١ قديم. خطتك جيدة — لكنها ناقصة.**

---

## الجزء ٢ — ما لم تذكره خطتك، وهو أخطر

### أ. ٦ شاشات لا يصلها أي زر
مسارات مسجّلة في `RafiqNavGraph` ومكتوبة بالكامل ولا مدخل لها من أي شاشة:
`Khatira` · `QuranAudioPlayer` · `PrayerTracking` · `CustomDhikr` · `DayCompanion` · `DeepLinkLanding`

### ب. ميزة الروابط العميقة **ديكور كامل**
`AndroidManifest.xml:41-46` يعلن `intent-filter` بـ `autoVerify="true"` لنطاق `rafiqaldhikr.app`.
لكن `MainActivity` **لا تقرأ الـ intent إطلاقاً**، ولا يوجد `navDeepLink` في الرسم البياني.
النتيجة: أندرويد يحاول التحقّق من النطاق ويفشل، وPlay يعرض تحذيراً، والميزة لا تعمل.
وبما أن `DeepLinkLandingScreen` هي **المدخل الوحيد** لـ `Breathing` و`RamadanHome`، فهاتان ميتتان عملياً أيضاً.

### ج. خدمة صوت القرآن تكلفة بلا مقابل
`QuranAudioService` مُعلَنة في الـmanifest مع `FOREGROUND_SERVICE_MEDIA_PLAYBACK` وثلاث مكتبات Media3 — والمسار المؤدي إليها يتيم. أي: إذن حسّاس + تدقيق Play إضافي + حجم، مقابل ميزة لا يمكن للمستخدم الوصول إليها.

### د. ملف إنجليزي ناقص
`values/strings.xml` = ٦٣ سلسلة · `values-en/strings.xml` = ٤٨. **١٥ سلسلة تسقط للعربية** في الواجهة الإنجليزية.

### هـ. APK بحجم ٣٥ ميغابايت مرفوع داخل المستودع
`dist/RafiqAlDhikr-v1.0-test.apk` — يضخّم كل استنساخ للمستودع بلا فائدة.

---

## الجزء ٣ — التشخيص الصريح

سألتَ: لماذا بعد ثلاثة أشهر لم تصل؟

**ليست جودة التنفيذ. المشكلة عدديّة.**

٤٥ مساراً · ٣١ مجلد ميزة · ٠ اختبار · ١٢ استخدام لـ `stringResource`.

هذا ليس تطبيقاً ناقصاً — هذه **ثلاثة تطبيقات، كلٌّ منها مكتمل بنسبة ٧٠٪**. وتطبيقٌ بنسبة ٧٠٪ في كل شيء يُحسّ رديئاً أكثر من تطبيقٍ بخمس شاشات مكتملة ١٠٠٪.

الدورة التي حبستك: كلما شعرتَ أن التطبيق «ليس مميزاً بعد» أضفتَ ميزة. والميزة الجديدة تسرق الوقت من إنهاء القديمة، فيزداد الإحساس بعدم الاكتمال، فتضيف ميزة أخرى.

**التمييز لا يأتي من عدد الميزات. يأتي من أن كل شيء يلمسه المستخدم يعمل بلا استثناء واحد.**

الخطة أدناه تكسر الدورة بقاعدة واحدة: **لن يُضاف شيء. سيُحذف الكثير.**

---

## الجزء ٤ — قرار النطاق (نهائي، غير قابل للنقاش داخل V1)

### يبقى — ٢٩ شاشة
اليوم (الورقة) · الأذكار: التصنيفات + القراءة + الاحتفاء · المسبحة · القرآن: القائمة + القراءة + البحث + العلامات · المواقيت + طريقة الحساب · القبلة · الأدعية: التصنيفات + القائمة + الدعاء الشعوري · أوراقي + الإحصاءات · الإعدادات: الإشعارات + المظهر + الخط + الوصول + اللغة · حول + المساعدة + الخصوصية + الشروط + التواصل + ما الجديد · التعريف الأول

### يُخفى — ٧ شاشات (الكود يبقى، المدخل يُزال)
`Garden` · `Breathing` · `Achievements` · `WeeklyReport` · `ShareCard` · `RamadanHome` · `WidgetSettings`
> سببها واحد: كلها جيدة، ولا واحدة منها ضرورية ليقول أحدٌ «هذا أفضل تطبيق أذكار استعملته».

### يُحذف — الكود نفسه
`Khatira` · `PrayerTracking` · `CustomDhikr` · مسار `DayCompanion` (الـViewModel يبقى، تستعمله الورقة) · `DeepLinkLanding` + الـ`intent-filter` · `Premium` + شاشتها · `wear` · `QuranAudioService` + أذونات الواجهة الأمامية + مكتبات Media3 · `SecurityModule.kt` · `dist/*.apk`
> الحذف هنا ليس تنظيفاً — كل بند منه يزيل إذناً أو تحذيراً أو نقطة فشل أمام Play.

**النتيجة: ٤٥ → ٢٩ شاشة. أقل بـ٣٦٪، وكلها تعمل.**

---

## الجزء ٥ — المراحل

### ط٠ — التجميد والتنظيف
- تنفيذ قرار النطاق أعلاه: حذف + إخفاء.
- إزالة `dist/` من المستودع.
- **معيار الإنجاز:** لا مسار يتيم واحد. سكربت تحقّق يُشغَّل في CI ويفشل إن ظهر واحد.

### ط١ — الثقة الدينية ⛔ حاجز نشر
هذا الأول لأن خطأ في وقت صلاة أسوأ من كل عيوب التطبيق مجتمعة.
- **حذف الاحتياطيّين معاً.** لا سليمانية ولا مكة. لا موقع = **لا مواقيت**، وشاشة تقول ذلك بوضوح وفيها زرّان: «تفعيل الموقع» و«اختيار المدينة يدوياً».
- توحيد مصدر الموقع في مكان واحد (`LocationSource`) يستهلكه الجميع: الشاشات والودجت وجدولة الإشعارات.
- الودجت والإشعارات: لا موقع = لا رقم مزيّف. الودجت يعرض «حدّد موقعك»، والجدولة تتوقف بدل جدولة أذان خاطئ.
- شارة تحت المواقيت: المدينة + طريقة الحساب + المذهب — ليرى المستخدم على أي أساس حُسِب.
- **جرد المحتوى الديني:** كل ذكر ودعاء وحديث يُطابَق بمصدره الموثّق ويُخزَّن المصدر مع النص ويُعرَض للمستخدم. أي نص بلا مصدر → يُحذف، لا يُصحَّح ولا يُعاد صياغته.
- **معيار الإنجاز:** ٠ إحداثية مكتوبة يدوياً في المستودع. ١٠٠٪ من النصوص الدينية لها مصدر معروض.

### ط٢ — الصدق القانوني ⛔ حاجز نشر
- إعادة كتابة سياسة الخصوصية لتصف الواقع: تخزين محلي غير مشفّر، لا خوادم، لا مزامنة، لا تتبّع. **حذف كل ذكر للتشفير والمزامنة.**
- `allowBackup="false"` + `dataExtractionRules` — أو تشفير حقيقي. الأول أصدق وأرخص.
- حذف `USE_EXACT_ALARM`، والاكتفاء بـ`SCHEDULE_EXACT_ALARM` مع مسار طلب لائق داخل التطبيق.
- **معيار الإنجاز:** كل جملة في السياسة يمكن إثباتها بسطر كود.

### ط٣ — الجاهزية التقنية ⛔ حاجز نشر
- `targetSdk = 36`.
- **إفشال البناء** عند غياب `KEYSTORE_FILE` في نسخة release بدل السقوط الصامت إلى debug.
- `bundleRelease` (AAB) + Play App Signing.
- فكّ قفل الاتجاه، ودعم الشاشة الكبيرة بمسار واحد بسيط (عمودان في القوائم).
- **معيار الإنجاز:** AAB موقّع يُبنى في CI. محاولة بناء release بلا مفتاح تفشل بخطأ واضح.

### ط٤ — اللغة الحقيقية
هذه المرحلة كبيرة وحدها — ~٧٠٠ سلسلة.
- استخراج كل نص واجهة إلى `strings.xml` (النصوص الدينية تبقى في القاعدة، لا تُترجَم ولا تُنقَل).
- إكمال `values-en` إلى ١٠٠٪.
- تقليص `locales_config.xml` إلى `ar` + `en`، وحصر شاشة اللغة بهما.
- **معيار الإنجاز:** فحص lint يفشل على أي نص عربي مكتوب في Kotlin خارج طبقة البيانات. ٠ سلسلة ناقصة في الإنجليزية.

### ط٥ — شبكة الأمان
- CI: `lint` + `test` + `assembleRelease`، وكلها حواجز تفشل البناء.
- اختبارات على ما لا يجوز أن ينكسر: حساب المواقيت، اتجاه القبلة، ترحيلات القاعدة، تعبئة البذر، منطق العدّاد.
- **الترحيلات:** استبدال `catch {}` بترحيل مُرقَّم صريح يسجّل ويرفع عند فشل غير متوقّع.
- **معيار الإنجاز:** CI أحمر عند كسر أي منها.

### ط٦ — الصقل
هنا فقط، وليس قبله:
- حركة فتح الصفحات · اهتزاز عند العدّ والإتمام · الحالات الفارغة كدعوة لا كفراغ · أول خمس ثوانٍ (التعريف) · القرآن والأدعية على مفهوم الورقة نفسه.

### ط٧ — البيتا ثم النشر
- بيتا مغلقة، ١٠–٢٠ شخصاً، أسبوعان، سؤال واحد: **«ما الشيء الذي أزعجك؟»**
- إصلاح ما تكرّر ذكره فقط. تجاهل طلبات الميزات كلها.
- ثم: الاسم والعلامة، لقطات المتجر، النشر.

---

## القاعدة الواحدة

**لا تنتقل من مرحلة إلى التي بعدها قبل أن يتحقّق معيار الإنجاز حرفياً.**

ط١ و ط٢ و ط٣ حواجز نشر: التطبيق **لا يُنشر** وواحد منها ناقص.
ط٤ و ط٥ يجعلانه قابلاً للصيانة.
ط٦ يجعله جميلاً — وهو آخر شيء، لا أوّله. هذا بالضبط ما انقلب عندك طوال ثلاثة أشهر.


---

## ملحق — الترجمة المحلية (أُضيف بعد ثلاث كسرات بناء في يوم)

### المشكلة
افترضتُ جلسةً كاملة أن الترجمة مستحيلة في هذه البيئة، فكان CI مترجمي
الوحيد ودورته ست دقائق. كسرتُ البناء ثلاث مرات، وكلّها أخطاء يمسكها
المترجم في ثوانٍ: فاصلة عليا غير مهروبة، ومكتبة في وحدة لا تملكها،
ورمز مستعمَل بلا استيراد.

**الافتراض كان خاطئاً.** لم أتحقّق منه قط. وحين تحقّقتُ:

| | |
|---|---|
| JDK 21 | موجود |
| Gradle | موجود |
| مساحة | ٣٠ جيجا فارغة |
| `dl.google.com` | متاح عبر الوكيل |
| **الناقص** | **Android SDK فقط — ٤٦٢ ميغابايت** |

### الحل
```bash
tools/setup_android_sdk.sh    # مرة واحدة، ~٥ دقائق
tools/verify.sh               # قبل كل دفعة
```

| | قبل | بعد |
|---|---|---|
| اكتشاف خطأ ترجمة | ~٦ دقائق (دورة CI) | **١٣ ثانية** |
| بلا تغيير | ~٦ دقائق | ٢ ثانية |

`verify.sh` يشغّل نفس ما يشغّله CI بالترتيب نفسه: الحرّاس الخمسة أولاً
(تكشف في ثانية ما يكلّف المترجم دقائق)، ثم الترجمة، ثم الاختبارات.

### وخطأ كشفه أوّل تشغيل محلي
أسماء دوال الاختبار كانت عربيةً بين backticks، وهي تُنتج ملفات `.class`
بأسماء عربية. فشلت هنا لأن محلّية الحاوية `POSIX` لا `UTF-8`:

```
InvalidPathException: Malformed input or input contains unmappable characters:
  …/GetPrayerTimesUseCaseTest$بلا إحداثيات يعطي خطأ موقع…$1.class
```

CI كان يمرّرها لأن محلّيته UTF-8 — فكانت هشاشةً مخبوءة تنتظر أول جهاز
بمحلّية أخرى، وويندوز شائع بين مطوّري أندرويد.

ضبط `LC_ALL` لم يكفِ (عامل Kotlin لا يرث الترميز)، والحلّ الصحيح ليس
حيلة بيئة بل إزالة الهشاشة: أسماء الدوال ASCII، والوصف العربي في KDoc
فوقها. الاختبارات تبقى مقروءة بالعربية وتبني في كل مكان.

### القاعدة
**لا دفعة قبل `tools/verify.sh` أخضر.** الحرّاس الخمسة تبقى في CI
كشبكة ثانية، لا كشبكة أولى.
