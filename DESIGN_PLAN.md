# نظام تصميم «رفيق الذكر» — الخطة الكاملة

> تدقيق فعلي على 46 شاشة و16,094 سطر واجهة، ثم خطة توحيد وتمييز على 7 مراحل.
> كل رقم في هذه الوثيقة مقيس من الكود، لا مقدَّر.

---

## ٠) التشخيص — الحكم بجملة واحدة

**التصميم الحالي ليس سيّئاً — هو افتراضي.**

زمرّدي + ذهبي + خلفية كريمية + نجمة ثمانية زخرفية + صور مساجد + بطاقات زجاجية
+ تدرّجات ذهبية: هذه هي المفردة البصرية التي تُخرِجها أي أداة AI لأي بريف
«تطبيق إسلامي». لا يوجد فيها ما يخصّ **رفيق الذكر** تحديداً.

الجودة الحرفية الموجودة عالية (لوحة ألوان موحّدة، أيقونات مرسومة، خلفية حيّة).
المشكلة ليست في التنفيذ — بل في أن **مصدر التميّز مأخوذ من الفئة العامة
(«إسلامي») لا من موضوع التطبيق نفسه («الوقت»)**.

### أرقام التدقيق

| البند | الحال | الصحّي | الأثر |
|---|---|---|---|
| `Text()` بلا `lineHeight` | **336 من 372** | 0 | 🔴 الأخطر |
| مقاسات خط مختلفة | **28** | 9 | 🔴 |
| قيم نصف قطر مختلفة | **15** | 4 | 🟠 |
| `RafiqShapes` المعرَّف | **0 استخدام** | كل مكان | 🟠 |
| شاشات تستخدم `RafiqTopBar` | **3 من 46** | 46 | 🔴 |
| مستويات ظل | **8** | 2 | 🟠 |
| ألوان أساس في اللوحة | **~12** | 5–6 | 🟠 |
| `tween` : `spring` | **20 : 5** | معكوسة | 🟡 |
| `reducedMotion` يُقرأ من الأنيميشن | **لا (0)** | نعم | 🔴 إتاحة |
| ملفات فيها `contentDescription` | **6** مقابل 106 `clickable` | كلها | 🔴 إتاحة |
| `letterSpacing` على نص عربي | **1** (`HomeScreen:403`) | 0 | 🟠 |

### التفاصيل الحرجة

**١. line-height — أعلى بند تأثير في التقرير كلّه.**
`RafiqTypography` معرَّف صحيحاً (`bodyLarge = 16/28`، `bodyMedium = 14/24`)، لكن
`MaterialTheme.typography` يُستخدم في **4 ملفات فقط**. الباقي يكتب
`fontSize = 16.sp` عارياً، فيرث `LocalTextStyle` الذي `lineHeight` فيه
`Unspecified`، فيرجع لمقاييس الخط (~1.3). والسبب التقني الدقيق في
`RafiqTheme.kt:97` — `ProvideTextStyle(...copy(fontFamily = UiFamily))` ينقل
**الخط فقط**، لا الـ`lineHeight`.

النتيجة: نصّ عربي مضغوط عمودياً في التطبيق كلّه. والمرجع العربي واضح — body
يحتاج **1.7–1.85**، والتشكيل يحتاج مساحة عمودية لا يعطيها اللاتيني.
رفع الـ`lineHeight` وحده يعطي أغلب التحسّن المحسوس في أي إعادة تصميم عربية.

**٢. الشريط العلوي.** `RafiqTopBar` موجود ويستخدمه 3 شاشات. الـ43 الباقية تكتب
`Row` يدوياً بـ`fontSize = 22.sp`. أي أن **حتى المكوّن الموحّد (20sp) لا يطابق
العُرف الفعلي (22sp)** — هذه ليست شاشات تخالف النظام، هذا نظام لا وجود له.

**٣. الحدود والظلال متنافسة.** نمطان: `border(1.dp, rc.divider)` (4 مرات)
و`border(1.dp, rc.gold @ 6–8%)` (38 مرة). ونفس البطاقة أحياناً بظلّ، أحياناً
بحدّ، وأحياناً بالاثنين معاً.

**٤. الخلفية `#F5F0E8`** قريبة جداً من `#F4F1EA` — الكريمي المعروف كعلامة
مخرجات AI. تُزاح.

---

## ١) الموضوع

**رفيق الذكر** · المسلم الناطق بالعربية — عشرات الزيارات القصيرة يومياً، بيد
واحدة، في أوقات محدّدة (بعد الفجر، قبل النوم) · **الوظيفة الوحيدة:** أن يقول لك
«الآن وقتُ هذا»، ويجعل فِعله ضغطةً واحدة.

مصدر التميّز من عالم الموضوع نفسه، لا من فئته في المتجر:

1. **الوقت والضوء.** أسماء الصلوات كلّها أسماء حالات ضوء: فجر، ضحى، ظهر، عصر،
   مغرب، عشاء. و«الميقات» مفهوم عربي إسلامي أصيل. الوقت هنا **بنية**، لا زخرفة.
2. **الطباعة العربية.** نصّ التطبيق كلّه عربي مشكول. الطباعة هي 80% من واجهته —
   فهي المكان الطبيعي لصوته.

---

## ٢) الإحساس المميّز

> **صفحةٌ تعرف الساعة.**
> تفتح التطبيق فيتغيّر ضوء الصفحة نفسها مع وقتك، والنصّ العربي يتنفّس مثل صفحة
> كتاب مطبوع لا مثل شاشة.

---

## ٣) القرار المعماري — جاذبية التقنية

Compose ⇒ الجاذبية الافتراضية هي **Material 3**.
**القرار: تبنٍّ مع تخصيص عميق للتوكنز** — لا Material خام، ولا Canvas مخصّص بالكامل.

- **لماذا لا Material خام:** يُخرِج تطبيق أندرويد عاماً، والبريف يرفض العام.
- **لماذا لا مخصّص بالكامل:** 46 شاشة تعمل اليوم؛ إعادة بنائها من Canvas تكلفة
  بلا عائد بصري يوازيها.
- **التخصيص الفعلي:** خمس طبقات توكنز (لون · ضوء · طباعة · شكل · حركة)
  + مكوّنات إجبارية تمنع الانحراف.

---

## ٤) Color — من 12 لوناً إلى 6

```
--night      #071A12   ← أعمق نقطة: عناوين الوضع الفاتح، وخلفية الوضع الداكن
--emerald    #09472B   ← الأساس: التنقّل، الأزرار، حالة «الآن»
--gold       #A97516   ← التمييز فقط: النصّ القرآني، الإنجاز، حدّ البطاقة النشطة
--paper      #F2EDE1   ← الخلفية (بدل #F5F0E8 — أدفأ وأقلّ حياداً)
--ash        #6E6455   ← النصّ الثانوي
--nightblue  #2E3A5C   ← المقابل البارد الوحيد: النوم والليل
+ حالة فقط:  --alert #B3261E
```

**يُحذف:** `purple`, `purpleSleep`, `accentIndigo`, `accentOrange`, `accentBrown`,
`blueAccent`, و«حلقات» الدلالة الأربع (`morningRing`, `eveningRing`, `sleepRing`,
`istighfarRing`).

**بديل التمييز بين الأقسام:** الأيقونة + **درجة الضوء** (الطبقة التالية)،
لا لون جديد لكلّ قسم. تطبيقٌ عن السكينة لا يملك ستّ درجات تمييز.

---

## ٥) طبقة الميقات — **هذا هو التوقيع**

`LocalMeeqat` — طبقة توكنز تُشتقّ من مواقيت الصلاة الحقيقية وتوفّر:

```kotlin
data class Meeqat(
    val phase: Phase,      // FAJR, DUHA, DHUHR, ASR, MAGHRIB, ISHA, LAYL
    val tint:  Color,      // لون ضوء هذا الوقت
    val t:     Float,      // موضع اللحظة داخل اليوم 0..1
)
```

يُطبَّق في **مكانين لا غير**:

1. **انحراف الورق.** `paper` ينحرف 2–4% نحو `tint`. الصفحة كلّها تتلوّن بضوء
   الوقت دون أن يشعر أحد أن هناك «خلفية».
2. **شريط الميقات.** خيط بارتفاع 3dp أسفل الشريط العلوي **في كل شاشة**، يمثّل
   اليوم كاملاً من الفجر إلى العشاء، وعليه علامة ذهبية بموضع «الآن».

```
┌──────────────────────────────────────────┐
│  ⚙            رَفِيقُ الذِّكر            🔔 │
│  ▬▬▬▬▬▬▬▬▬▬▬◆▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬  │ ← شريط الميقات
├──────────────────────────────────────────┤
│         بِسْمِ اللَّهِ الرَّحْمَنِ الرَّحِيمِ          │
│                                          │
│  رفيق اليوم ▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬·  │ ← علامة الكشيدة
│  ┌────────────────────────────────────┐  │
│  │ الآن · العصر وأذكار المساء          │  │
│  │ تنتهي النافذة بعد ٠٢:٣٧:٥٨          │  │
│  │ [           ابدأ الآن           ]  │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

### ما يحلّ محلّه (نصيحة شانيل مطبَّقة: شِل إكسسواراً واحداً)

| يُحذف | لماذا |
|---|---|
| `LivingSkyBackground` (شمس/هلال/نجوم مرسومة) | يقول الوقت زخرفياً |
| `bg_time_*.webp` — أربع صور مساجد | صور مخزون، لا تخصّ التطبيق |
| `PrayerScene` | تكرار ثالث لنفس الرسالة |
| `GeomStar` كعلامة مائية في كل بطاقة | زخرفة لا تشفّر شيئاً |

التطبيق الحالي يقول «الوقت» **بأربع طرق زخرفية متنافسة**. الجديد يقوله بطريقة
واحدة، وتكون **بيانات**: تُقاس، وتُلمَس فتنقلك.

---

## ٦) Type — السلّم وقواعد العربية

| الغرض | الخط |
|---|---|
| البسملة والآيات القصيرة | **Amiri** |
| المصحف | **Scheherazade New** |
| الأذكار والأدعية المشكولة | **Noto Naskh Arabic** |
| الواجهة والأرقام | **Noto Sans Arabic** + `tnum` |

### السلّم — 11 نمطاً مسمّى بدل 28 مقاساً عشوائياً

| النمط | الحجم | line-height | النسبة | الاستخدام |
|---|---|---|---|---|
| `quran`   | 24 | **46** | 1.92 | المصحف — التشكيل العثماني يحتاج الأوسع |
| `dhikr`   | 20 | **36** | 1.80 | نصّ الذكر المشكول |
| `ayah`    | 26 | 42 | 1.62 | البسملة، الآية المقتبسة |
| `display` | 32 | 44 | 1.38 | عدّاد المسبحة — المكان الوحيد لرقم ضخم |
| `titleL`  | 22 | 32 | 1.45 | عنوان الشاشة |
| `titleM`  | 18 | 27 | 1.50 | عنوان بطاقة |
| `body`    | 16 | **28** | **1.75** | النصّ العادي |
| `bodyS`   | 14 | **25** | **1.79** | النصّ الثانوي |
| `label`   | 15 | 23 | 1.53 | عنوان قسم، نصّ زر |
| `caption` | 12 | 20 | 1.67 | تسمية صغيرة |
| `micro`   | 11 | 17 | 1.55 | شارة — الحدّ الأدنى المطلق |

### قواعد إلزامية

- **ممنوع `fontSize =` عارياً.** كل نصّ: `style = RafiqType.body`. تُفحص آلياً.
- **ممنوع `letterSpacing` على أي نصّ عربي** — الحروف متّصلة والتباعد يكسر الكلمة.
  (يوجد واحد الآن: `HomeScreen.kt:403` على عنوان التطبيق.)
- `textAlign = TextAlign.Start` لا `End`؛ و`start/end` padding لا `left/right`.
- المقاسات دون 11sp (9.5 · 10) تُرقّى إلى `micro`.
- العناوين والأزرار **+10%** مقابل نظيرها اللاتيني — العربية بلا capitals فتفقد
  الثقل البصري.

---

## ٧) Shape — 4 قيم بدل 15

```
r-chip  =  999.dp   كبسولة كاملة — الشارات والحبوب
r-item  =   12.dp   أيقونة داخل صفّ، حقل إدخال، زر صغير
r-card  =   20.dp   كل البطاقات (وهي القيمة الأشيع أصلاً: 117 مرة)
r-sheet =   28.dp   الشيتات والحوارات
```

`RafiqShapes` يُحدَّث لهذه القيم، وكل `RoundedCornerShape(N.dp)` مكتوب inline
يُحذف. مفتاح التوحيد: **الشكل يشفّر الوظيفة** (حبّة/عنصر/بطاقة/طبقة)، لا المزاج.

---

## ٨) الارتفاع — نظامان بدل ثمانية

**البطاقات بلا ظلّ.** الفصل بالحدّ وحده:

```
بطاقة ساكنة   →  border 1dp · gold @ 8%
بطاقة نشطة    →  border 1dp · gold @ 45%     ← الحالة الوحيدة التي «تلمع»
طبقة عائمة    →  shadow 12dp                  ← الشيت والبوتوم بار فقط
```

الظلّ فوق خلفية ورقية دافئة يُخرِج رمادياً متّسخاً. الحدّ الذهبي أنظف، وأقرب
لتذهيب المخطوط.

---

## ٩) المسافات — سلّم واحد

`4 · 8 · 12 · 16 · 24 · 32 · 48`

هامش الشاشة **16** ثابتاً · بين البطاقات **12** · بين الأقسام **24**.
(الحال اليوم: 61 مرة `14.dp` و55 مرة `16.dp` تتنافسان على نفس الدور.)

---

## ١٠) الحركة

```kotlin
tap      spring(dampingRatio = .65f, stiffness = Medium)   // كل ضغطة
enter    spring(dampingRatio = .90f, stiffness = Low)
progress tween(700, FastOutSlowInEasing)                   // القيم العددية فقط
```

- كل حركة **مكانية** = `spring` (قابلة للمقاطعة). `tween` للّون والتقدّم فقط.
- **`LocalReducedMotion`** يُقرأ من مصدرين: إعداد المستخدم الموجود أصلاً في
  `SettingsViewModel`، و`Settings.Global.ANIMATOR_DURATION_SCALE == 0`.
  عند التفعيل ترجع كل الحركات `snap()`.
  اليوم: الإعداد موجود في الشاشة و**لا يقرؤه أي أنيميشن** — أي أن الإعداد يكذب
  على المستخدم. هذا خلل إتاحة، لا نقص ميزة.
- **حركة لانهائية واحدة في التطبيق كلّه**: علامة «الآن» على شريط الميقات.
  (اليوم: ستّ حركات لانهائية تعمل معاً على الرئيسية — بطارية وتشتيت.)

---

## ١١) المكوّنات الإجبارية — الحوكمة

| المكوّن | الحال | الهدف |
|---|---|---|
| `RafiqTopBar` | 3 / 46 | **46 / 46** — ارتفاع موحّد + شريط الميقات تحته |
| `RafiqCard` | غير موجود | يستبدل 40+ نسخة يدوية من `clip + background + border` |
| `RafiqSection` | `SecLabel` خاصّ بالرئيسية | مشترك، بعلامة الكشيدة |
| `RafiqListRow` | مكرَّر في 8 شاشات | واحد |
| `EmptyState` / `LoadingState` / `ErrorState` | 8 / 46 | كل شاشة فيها بيانات |

القاعدة: **الشاشة لا تملك حقّ رسم بطاقة أو شريط علوي.** تطلبه من النظام.

---

## ١٢) علامة القسم — الكشيدة

`SecLabel` اليوم: مستطيل ذهبي 4×20dp. عامّ، يعمل بأي لغة، ولا يقول شيئاً.

البديل: **خطّ الكشيدة** — المدّة الأفقية التي تصل الحرف العربي بالذي بعده —
يمتدّ من نهاية عنوان القسم إلى طرف الشاشة، بسماكة 1.5dp ذهبي @35%، وينتهي
بنقطة. مأخوذ من الخطّ العربي نفسه، لا يُفهم بلغة أخرى، ولا يكلّف مساحة.

```
رفيق اليوم ▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬·
```

---

## ١٣) النصوص — جزء من التصميم

- فعل مضارع مباشر من جهة المستخدم: «ابدأ الأذكار» لا «فتح شاشة الأذكار».
- الاسم نفسه في كل المسار: زرّ «سجّل الذكر» ⇐ رسالة «تمّ تسجيل الذكر».
- الشاشة الفارغة **دعوة لفعل**: «ما حفظتَ آية بعد — افتح المصحف واضغط العلامة»
  بدل «لا توجد بيانات».
- الخطأ يقول ما حدث وكيف يُحلّ: «ما قدرنا نحدّد موقعك — المواقيت الآن تقريبية.
  فعّل الموقع من الإعدادات لدقّة كاملة.»

---

## ١٤) الإتاحة — شرط تسليم لا ميزة

- `contentDescription` لكل أيقونة معلوماتية، و`null` صريح للزخرفية.
  (اليوم: 6 ملفات فقط مقابل 106 عنصر قابل للنقر — قارئ الشاشة يرى صفحة فارغة.)
- كل `clickable` بمساحة لمس **≥48dp** عبر `minimumInteractiveComponentSize()`
  مهما صغُر الشكل البصري. (اليوم: 18 عنصراً بأحجام 14–36dp.)
- تباين **4.5:1** للنصّ العادي و**3:1** للكبير — فحص آليّ على كل زوج في اللوحة.
- تكبير خطّ النظام حتى **200%** بلا قصّ ولا تداخل.

---

## ١٥) خارطة التنفيذ — 7 مراحل، كل مرحلة تُبنى وتُختبر وحدها

| # | المرحلة | العائد | الخطر |
|---|---|---|---|
| **م0** | **البسملة** — نصّ وحده بلا إطار ولا زخرفة | ✅ **منفَّذة ومبنيّة** | — |
| **م1** | **الطباعة**: `RafiqType` + إجبار `lineHeight` | ✅ **منفَّذة ومبنيّة** | — |
| **م2** | **الشكل والارتفاع**: `RafiqShape` + `RafiqCard` + حذف الظلال | ✅ **منفَّذة** | — |
| **م3** | **اللون**: تقليص اللوحة من 48 توكن إلى ~20 | متوسط | متوسط |
| **م4** | **التوقيع**: `LocalMeeqat` + شريط الميقات + حذف الخلفيات الأربع | 🔥 التمييز كلّه | متوسط |
| **م5** | **الحوكمة**: `RafiqTopBar` على 46 شاشة + المكوّنات المشتركة | عالٍ | متوسط |
| **م6** | **الحركة والإتاحة**: spring + `LocalReducedMotion` + semantics + مساحات اللمس | عالٍ | منخفض |
| **م7** | **المراجعة**: لقطات قبل/بعد لكل شاشة + فحص تباين آلي + اختبار 200% | — | — |

**م1 أولاً عن قصد:** أعلى عائد محسوس بأقلّ خطر بنيوي — لا شاشة تتغيّر بنيتها،
والنصّ العربي كلّه يبدأ يتنفّس.

---

## ١٦) اختبار الافتراضي — النقد قبل التنفيذ

**السؤال:** لو جاءني الطلب نفسه بموضوع مختلف تماماً — تطبيق تأمّل ياباني —
هل أصل إلى النتيجة نفسها؟

- ✅ **سلالم الطباعة والشكل والحركة: نعم** — وهذا صحيح. هذه حِرفة، لا هويّة.
- ❌ **طبقة الميقات: لا.** التأمّل الياباني لا يملك يوماً مقسَّماً إلى نوافذ
  مسمّاة بحالات ضوء. الميقات مأخوذ من عالم الموضوع نفسه.
- ❌ **علامة الكشيدة: لا.** مأخوذة من الخطّ العربي.

### ما رسب فعلاً، وما غُيِّر

**رسب ١ — بطاقة البطل.** كانت المسوّدة الأولى تبقي على «بطاقة الصلاة القادمة»
كما هي: تدرّج + رقم 44sp + إحصائيات. وهذا بالضبط نمط
«Hero برقم كبير + label + gradient» المحذَّر منه، وهو موجود في التطبيق اليوم.
**التغيير:** الصلاة القادمة تصير صفّاً واحداً هادئاً مقروءاً داخل شريط الميقات،
والرقم الضخم يُحصر في المكان الوحيد الذي يحتاجه فعلاً: **عدّاد المسبحة**.

**رسب ٢ — الكريمي.** `#F5F0E8` قريب من `#F4F1EA`، وهو اللون الذي يُقرأ فوراً
كمخرَج AI. **التغيير:** `#F2EDE1` — أدفأ، أقلّ حياداً، وأقرب لورق المخطوط.

**رسب ٣ — النجمة الثمانية.** كانت ستبقى «رمز الهوية». لكنها لا تشفّر شيئاً في
المحتوى: توضع خلف بطاقة الورد وبطاقة البسملة بلا معنى مختلف. **التغيير:** تُحذف
كعلامة مائية، وتبقى فقط حيث تعني شيئاً — أيقونة داخل مجموعة الأيقونات.

---

## ١٧) قائمة ما قبل التسليم

- [x] قرار «جاذبية التقنية» اختيار واعٍ من البريف؟ — Material + تخصيص عميق، والسبب مكتوب (§3).
- [x] عنصر توقيع واحد واضح، والجرأة كلّها مصروفة عليه؟ — الميقات، وقد حُذفت أربعة أجهزة زخرفية مقابله (§5).
- [x] اختبار الافتراضي أُجري وعُدِّل ما رسب؟ — ثلاثة بنود رسبت (§16).
- [x] العربية: `line-height` والخطوط مقابل مرجع RTL؟ — body 1.75 · dhikr 1.80 · quran 1.92 (§6).
- [x] الحركة مقصودة في مكان واحد لا متناثرة؟ — حركة لانهائية واحدة بدل ست (§10).
- [x] الكوبي بصوت هذا المنتج بالذات؟ — §13.
